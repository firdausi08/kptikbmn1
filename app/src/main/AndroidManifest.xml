<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.afip.kptikbmn1">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="KPTIK BMN Surabaya"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LaporActivity" />
        <activity android:name=".RiwayatActivity" />
        <activity android:name=".Ruangan" />
        <activity android:name=".RiwayatRuangan" />
        <activity android:name=".FormRuangan" />
        <activity
            android:name=".Ruangan2"
            android:label="@string/title_activity_ruangan2"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>